def options(opt):
   pass

def configure(cnf):
   pass

def build(bld):
   bld.stlib(
      source       = ["Code/tlsf.c"], 

      target       = 'TLSF', 
      use          = [''],

      includes     = ["Code"],
      defines      = [],

      lib          = [],
      libpath      = [],

      rpath        = [],
      vnum         = '1.2.3',

      idx          = 123,
      #install_path = '${SOME_PATH}/bin',
      cxxflags     = [],
      linkflags    = []
   )